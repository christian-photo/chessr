[package]
name = "chessr"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
criterion = { version = "0.8" }

[features]
default = ["pext"]
pext = []

[[bench]]
name = "benchmark"
harness = false

[dependencies]
bitintr = "0.3.0"
vampirc-uci = "0.11"
rand = "0.10.1"
